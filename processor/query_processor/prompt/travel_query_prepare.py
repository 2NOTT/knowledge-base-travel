"""旅游问题改写与实体提取提示词。"""

TRAVEL_QUERY_SYSTEM_PROMPT = "你是旅游知识库的问题分析助手，只返回合法 JSON。"

TRAVEL_QUERY_TEMPLATE = """
历史会话：
{history_text}

当前用户问题：
{query}

请结合历史会话，整理一个可以独立检索的旅游问题，并提取明确的旅游实体。
实体可以是城市、景点、线路、住宿区域、餐饮主题等；没有明确实体时返回空数组。
不要因为没有实体而拒绝问题，泛化问题也必须保留并继续检索。

请严格返回 JSON：
{{
  "travel_entities": ["成都", "熊猫基地"],
  "cities": ["成都"],
  "rewritten_query": "成都有哪些适合亲子游客的景点？"
}}
"""
